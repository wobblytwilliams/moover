# Optimise a moover Model

Runs the RF optimisation workflow and writes candidate tables, plots,
and profile selections into the current run.

## Usage

``` r
optimise_model(spec)
```

## Arguments

- spec:

  A `moover_spec` object or path to a JSON spec.

## Value

The optimisation output directory.
