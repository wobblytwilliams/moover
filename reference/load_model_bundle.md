# Load a moover Model Bundle

Loads an exported bundle from disk for prediction or inspection.

## Usage

``` r
load_model_bundle(path)
```

## Arguments

- path:

  Path to an exported model bundle directory.

## Value

A list with class `moover_model_bundle`.
