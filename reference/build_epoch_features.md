# Build Epoch Features

Converts configured labelled data into the canonical epoch feature
dataset used by training and optimisation.

## Usage

``` r
build_epoch_features(spec)
```

## Arguments

- spec:

  A `moover_spec` object or path to a JSON spec.

## Value

The path to the canonical epoch feature dataset.
