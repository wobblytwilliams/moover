# Guided Import Wizard

Interactively builds a simple import spec, previews the canonicalised
data, and writes the run spec into the current workspace.

## Usage

``` r
wizard_import()
```

## Value

A `moover_spec` object.
